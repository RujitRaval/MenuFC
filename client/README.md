# client/ — SwiftBar plugin `menufc.30s.py`

**Phase 3.** Python 3 (stdlib only), crash-proof, calls the Worker's `/scores` endpoint — never
football-data.org directly. Contains no secret; safe to open-source.

> Not built yet — placeholder so the folder is tracked.
